// FEASTA's customer-mobile route policy is deliberately framework-neutral
// while legacy screens are migrated from imperative Navigator calls. The
// authentication gate consumes this policy as the guarded navigation layer.
export 'customer_route_guard.dart';
